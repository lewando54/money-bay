import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    base:{
        padding: 8,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
})